openapi: 3.0.3
info:
  title: Papo API
  description: |
    API do Papo — chat self-hosted (Go + Echo + PostgreSQL + WebSockets).

    Convenções:
    - Autenticação via cookie HttpOnly `Auth` (JWT).
    - Erros seguem RFC 7807 (`application/problem+json`) e toda resposta
      inclui o header `X-Request-ID`.
  version: 0.9.7
servers:
  - url: http://localhost:8080
    description: Backend local
tags:
  - name: Health
  - name: Auth
  - name: Users
  - name: Servers
  - name: Channels
  - name: Messages
  - name: Notifications
  - name: Roles
  - name: UserRoles
  - name: Emojis
  - name: Attachments
  - name: Media
  - name: LinkPreviews
  - name: Search
  - name: Admin
  - name: Voice
  - name: WebSocket

security:
  - cookieAuth: []

paths:
  /health:
    get:
      tags: [Health]
      summary: Health check
      security: []
      responses:
        '200':
          description: Servidor no ar
          content:
            text/plain:
              example: OK

  /auth/register:
    post:
      tags: [Auth]
      summary: Cria novo usuário
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterRequest'
      responses:
        '201':
          description: Usuário criado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Servidor não público sem autorização temporária (execute /auth/login_server antes)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          $ref: '#/components/responses/Banned'
        '409':
          description: Username já existe
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'

  /auth/login:
    post:
      tags: [Auth]
      summary: Faz login e define o cookie Auth (HttpOnly)
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: "Login bem-sucedido (Set-Cookie: Auth=jwt)"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Credenciais inválidas ou servidor não público sem autorização temporária (execute /auth/login_server antes)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          $ref: '#/components/responses/Banned'
        '500':
          $ref: '#/components/responses/InternalError'

  /auth/login_server:
    post:
      tags: [Auth]
      summary: Valida a senha do servidor e define o cookie Auth com token temporário (30min)
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginServerRequest'
      responses:
        '204':
          description: "Senha válida (Set-Cookie: Auth=temp_token; Max-Age=1800). O token não é retornado no corpo; a autorização é entregue exclusivamente pelo cookie Auth (HttpOnly)"
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Senha do servidor incorreta
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          $ref: '#/components/responses/Banned'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /auth/whoami:
    get:
      tags: [Auth]
      summary: Retorna o usuário autenticado com settings
      responses:
        '200':
          description: Usuário autenticado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WhoamiResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'

  /auth/logout:
    post:
      tags: [Auth]
      summary: Faz logout (revoga a conexão de sessão no banco e remove o cookie Auth)
      security: []
      responses:
        '204':
          description: Logout bem-sucedido (Set-Cookie expira o Auth)

  /auth/refresh:
    post:
      tags: [Auth]
      summary: Rotaciona o token de sessão (novo JWT + Set-Cookie) e retorna a conexão atual
      responses:
        '200':
          description: "Sessão rotacionada (Set-Cookie: Auth=novo_jwt)"
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RefreshResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'

  /auth/connected_devices:
    get:
      tags: [Auth]
      summary: Lista as conexões de sessão ativas do usuário
      responses:
        '200':
          description: Conexões ativas
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConnectedDevicesResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'

  /auth/drop_connection:
    post:
      tags: [Auth]
      summary: Revoga uma conexão de sessão (id específico ou ALL para todas)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DropConnectionRequest'
      responses:
        '200':
          description: Conexão(ões) revogada(s)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DropConnectionResponse'
        '400':
          description: connection_id ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/profile:
    get:
      tags: [Users]
      summary: Perfil do usuário (com avatar); qualquer usuário autenticado pode consultar
      parameters:
        - $ref: '#/components/parameters/UserId'
      responses:
        '200':
          description: Perfil do usuário
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfile'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/profile_batch:
    post:
      tags: [Users]
      summary: Perfis em lote (mesma forma do profile; máx. 50 ids; ids inexistentes são pulados)
      description: |
        Retorna os perfis dos usuários solicitados, na ordem da requisição,
        pulando ids que não existem. Ids duplicados são considerados uma única
        vez. Máximo de 50 ids por requisição.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProfileBatchRequest'
      responses:
        '200':
          description: Perfis dos usuários encontrados (na ordem da requisição)
          content:
            application/json:
              schema:
                type: object
                properties:
                  profiles:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserProfile'
        '400':
          description: Campo ausente ou inválido (ids ausente/vazio, id vazio ou mais de 50 ids)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'

  /users:
    get:
      tags: [Users]
      summary: Lista usuários (Limite de 100 por requisição, use cursor com since + last_id para paginar)
      parameters:
        - name: since
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Timestamp ISO 8601 (polling de novos usuários)
        - name: last_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: ID do último usuário da página anterior (cursor exato usado com since)
      responses:
        '200':
          description: Lista de usuários
          content:
            application/json:
              schema:
                type: object
                properties:
                  users:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserSummary'
                  has_more:
                    type: boolean
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/settings:
    put:
      tags: [Users]
      summary: Atualiza as configurações do usuário autenticado
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [config]
              properties:
                config:
                  $ref: '#/components/schemas/UserConfig'
      responses:
        '200':
          description: Configurações atualizadas
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettings'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}:
    put:
      tags: [Users]
      summary: Atualiza nickname, status, description e typing (somente o próprio usuário)
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserRequest'
      responses:
        '200':
          description: Perfil atualizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericResponse'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/avatar:
    put:
      tags: [Users]
      summary: Atualiza o avatar (base64, GIF/JPEG/JPG/PNG/WEBP, máx. 2MB, 512px)
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAvatarRequest'
      responses:
        '200':
          description: Avatar atualizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericResponse'
        '400':
          description: Avatar inválido (base64 de GIF/JPEG/JPG/PNG/WEBP até 2MB)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/banner:
    put:
      tags: [Users]
      summary: Atualiza o banner (base64, GIF/JPEG/JPG/PNG/WEBP, máx. 2MB, 512px); corpo vazio remove o banner
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBannerRequest'
      responses:
        '200':
          description: Banner atualizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericResponse'
        '400':
          description: Banner inválido (base64 de GIF/JPEG/JPG/PNG/WEBP até 2MB)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/status:
    put:
      tags: [Users]
      summary: Atualiza o status persistido (away/busy; null remove)
      description: >
        O status vale enquanto o usuário estiver online (disponibilizado via
        presence_update/presence_sync); offline, o status persistido vale na
        próxima conexão.
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateStatusRequest'
      responses:
        '200':
          description: Status atualizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericResponse'
        '400':
          description: Status inválido (deve ser away, busy ou null)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/password:
    put:
      tags: [Users]
      summary: Altera a própria senha e reinicia a flag de reset de senha
      description: Senha precisa ter no mínimo 8 caracteres, 1 letra maíscula + 1 caractere especial
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordRequest'
      responses:
        '200':
          description: Senha atualizada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericResponse'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/ban:
    put:
      tags: [Users]
      summary: Bane ou desbane um usuário (banimento global)
      description: "Permission Check: dono de um servidor ou `manage_server`"
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BanUserRequest'
      responses:
        '200':
          description: Estado de banimento alterado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericResponse'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/reset:
    post:
      tags: [Users]
      summary: Marca o usuário para redefinir a senha
      description: "Permission Check: dono de um servidor ou o próprio usuário"
      parameters:
        - $ref: '#/components/parameters/UserId'
      responses:
        '200':
          description: Flag de reset de senha definida
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GenericResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/roles:
    post:
      tags: [UserRoles]
      summary: Atribui role a um usuário
      description: "Permission Check: `manage_roles`"
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AssignRoleRequest'
      responses:
        '201':
          description: Role atribuída
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRole'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/roles/{role_id}:
    delete:
      tags: [UserRoles]
      summary: Remove role de um usuário
      description: "Permission Check: `manage_roles`"
      parameters:
        - $ref: '#/components/parameters/UserId'
        - $ref: '#/components/parameters/RoleId'
      responses:
        '204':
          description: Role removida
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/push_registrations/{installation_id}:
    put:
      tags: [Notifications]
      summary: Registra uma capability de push desta instalação
      description: >
        Registra uma capability delegada para entrega via Papo Push Relay.
        O backend nunca recebe token FCM nem credenciais Firebase. address e
        auth_token são write-only e ligados à sessão autenticada atual.
      parameters:
        - $ref: '#/components/parameters/UserId'
        - name: installation_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PushRegistrationRequest'
      responses:
        '200':
          description: Registration criada ou atualizada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PushRegistration'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      tags: [Notifications]
      summary: Remove a capability de push desta instalação
      parameters:
        - $ref: '#/components/parameters/UserId'
        - name: installation_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Registration ausente ou removida
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/notifications:
    get:
      tags: [Notifications]
      summary: Lista as notificações do usuário em ordem decrescente (Limite de 100 por requisição, use cursor com since + last_id para paginar)
      description: "Permission Check: somente o próprio usuário (user_id autenticado)"
      parameters:
        - $ref: '#/components/parameters/UserId'
        - name: since
          in: query
          required: false
          description: Timestamp ISO 8601 para polling de novas notificações
          schema:
            type: string
            format: date-time
        - name: last_id
          in: query
          required: false
          description: Id da última notificação da página anterior; usado com since como cursor exato (created_at, id)
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Notificações do usuário
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationList'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalError'

  /users/{user_id}/read_notification:
    put:
      tags: [Notifications]
      summary: Marca notificações do usuário como lidas (1 a 1000 ids)
      description: "Permission Check: somente o próprio usuário (user_id autenticado)"
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MarkNotificationsReadRequest'
      responses:
        '200':
          description: Notificações marcadas como lidas
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MarkNotificationsReadResponse'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /server:
    get:
      tags: [Servers]
      summary: Detalhes do servidor
      security: []
      responses:
        '200':
          description: Detalhes do servidor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Server'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      tags: [Servers]
      summary: Cria o servidor
      description: "Permission Check: Table Servers have no registry (somente 1 servidor)"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateServerRequest'
      responses:
        '200':
          description: Servidor criado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateServerResponse'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '409':
          description: O servidor já foi criado (máximo de 1)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'
    put:
      tags: [Servers]
      summary: Altera o servidor (nome, ícone, senha e visibilidade)
      description: "Permission Check: dono do servidor ou `manage_server`"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateServerRequest'
      responses:
        '200':
          description: Servidor atualizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Server'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /roles:
    get:
      tags: [Roles]
      summary: Lista roles
      responses:
        '200':
          description: Lista de roles
          content:
            application/json:
              schema:
                type: object
                properties:
                  roles:
                    type: array
                    items:
                      $ref: '#/components/schemas/Role'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      tags: [Roles]
      summary: Cria role
      description: "Permission Check: `manage_roles`"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRoleRequest'
      responses:
        '201':
          description: Role criada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Role'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: Nome de role já existe
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'

  /roles/{role_id}:
    put:
      tags: [Roles]
      summary: Atualiza role
      description: "Permission Check: `manage_roles`"
      parameters:
        - $ref: '#/components/parameters/RoleId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRoleRequest'
      responses:
         '200':
           description: Role atualizada
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Role'
         '400':
           description: Campo ausente ou inválido
           content:
             application/problem+json:
               schema:
                 $ref: '#/components/schemas/Error'
         '401':
           $ref: '#/components/responses/Unauthorized'
         '403':
           $ref: '#/components/responses/Forbidden'
         '404':
           $ref: '#/components/responses/NotFound'
         '409':
           description: Nome de role já existe
           content:
             application/problem+json:
               schema:
                 $ref: '#/components/schemas/Error'
         '500':
           $ref: '#/components/responses/InternalError'
    delete:
      tags: [Roles]
      summary: Exclui role
      description: "Permission Check: `manage_roles`"
      parameters:
        - $ref: '#/components/parameters/RoleId'
      responses:
        '204':
          description: Role excluída
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /channels:
    get:
      tags: [Channels]
      summary: Lista canais
      responses:
        '200':
          description: Lista de canais
          content:
            application/json:
              schema:
                type: object
                properties:
                  channels:
                    type: array
                    items:
                      $ref: '#/components/schemas/Channel'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      tags: [Channels]
      summary: Cria canal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateChannelRequest'
      responses:
        '201':
          description: Canal criado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Channel'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: Nome de canal já existe ou limite de canais atingido (500)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'

  /channels/{channel_id}:
    put:
      tags: [Channels]
      summary: Edita canal
      description: "Permission Check: `manage_channels`"
      parameters:
        - $ref: '#/components/parameters/ChannelId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateChannelRequest'
      responses:
        '200':
          description: Canal atualizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Channel'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Nome de canal já existe
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      tags: [Channels]
      summary: Remove canal
      description: "Permission Check: `manage_channels`"
      parameters:
        - $ref: '#/components/parameters/ChannelId'
      responses:
        '204':
          description: Canal removido
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /channels/{channel_id}/change_position:
    put:
      tags: [Channels]
      summary: Altera a posição do canal
      description: "Permission Check: `manage_channels`"
      parameters:
        - $ref: '#/components/parameters/ChannelId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangeChannelPositionRequest'
      responses:
        '200':
          description: Posição atualizada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Channel'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Posição do canal desatualizada
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'

  /channels/{channel_id}/permissions:
    get:
      tags: [Channels]
      summary: Lê permissões do canal por role
      parameters:
        - $ref: '#/components/parameters/ChannelId'
      responses:
        '200':
          description: Permissões do canal
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChannelPermissionsResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /channels/{channel_id}/permissions/{role_id}:
    put:
      tags: [Channels]
      summary: Altera permissões de uma role no canal
      description: "Permission Check: `manage_channels`"
      parameters:
        - $ref: '#/components/parameters/ChannelId'
        - $ref: '#/components/parameters/RoleId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateChannelPermissionsRequest'
      responses:
        '200':
          description: Permissões atualizadas
          content:
            application/json:
              schema:
                type: object
                properties:
                  channel_id:
                    type: string
                    format: uuid
                  role_id:
                    type: string
                    format: uuid
                  permissions:
                    $ref: '#/components/schemas/ChannelPermissions'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /channels/{channel_id}/user/{user_id}/settings:
    post:
      tags: [Notifications]
      summary: Define a configuração de notificação do usuário no canal
      description: "Permission Check: somente o próprio usuário (user_id autenticado)"
      parameters:
        - $ref: '#/components/parameters/ChannelId'
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateChannelUserSettingRequest'
      responses:
        '200':
          description: Configuração atualizada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChannelUserSetting'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /voice/ice-servers:
    get:
      tags: [Voice]
      summary: Servidores ICE (STUN/TURN) do usuário
      description: >
        Retorna a lista de servidores ICE do usuário autenticado, no mesmo shape
        de RTCIceServer do browser (passar direto para
        new RTCPeerConnection({ iceServers })). Quando TURN REST está configurado,
        as credenciais são efêmeras e compatíveis com coturn
        (username com expiração + Base64 HMAC-SHA1).
      responses:
        '200':
          description: Servidores ICE
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ICEServersResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'

  /channels/{channel_id}/messages:
    get:
      tags: [Messages]
      summary: Lista mensagens do canal em ordem decrescente (Limite de 100 por Requisição, use cursor com since + last_id para paginar)
      parameters:
        - $ref: '#/components/parameters/ChannelId'
        - name: since
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Timestamp ISO 8601 (polling de novas mensagens)
        - name: last_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: ID da última mensagem da página anterior (cursor exato usado com since)
      responses:
        '200':
          description: Mensagens do canal
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageListResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /messages:
    post:
      tags: [Messages]
      summary: Envia mensagem
      description: "Permission Check: `send_messages`"
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/CreateMessageRequest'
      responses:
        '201':
          description: Mensagem criada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Message'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /messages/{message_id}:
    put:
      tags: [Messages]
      summary: Edita mensagem
      description: "Permission Check: autor da mensagem"
      parameters:
        - $ref: '#/components/parameters/MessageId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMessageRequest'
      responses:
        '200':
          description: Mensagem editada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Message'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      tags: [Messages]
      summary: Remove mensagem
      description: "Permission Check: autor da mensagem ou `delete_messages`"
      parameters:
        - $ref: '#/components/parameters/MessageId'
      responses:
        '204':
          description: Mensagem removida
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
           $ref: '#/components/responses/InternalError'

  /channels/{channel_id}/messages/{message_id}/pin:
    post:
      tags: [Messages]
      summary: Fixa mensagem no canal
      description: "Permission Check: `pin_message` (dono do servidor ou role com a permissão). Idempotente: fixar uma mensagem já pinada retorna 200. Limite de 100 mensagens pinadas por canal."
      parameters:
        - $ref: '#/components/parameters/ChannelId'
        - $ref: '#/components/parameters/MessageId'
      responses:
        '200':
          description: Mensagem já estava pinada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PinnedMessage'
        '201':
          description: Mensagem fixada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PinnedMessage'
        '400':
          description: Parâmetro ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
           description: Limite de 100 mensagens pinadas atingido
           content:
             application/problem+json:
               schema:
                 $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      tags: [Messages]
      summary: Remove a fixação da mensagem no canal
      description: "Permission Check: `read_channel` do canal e `pin_message` (dono do servidor ou role com a permissão). Não é idempotente: mensagem não pinada retorna 404."
      parameters:
        - $ref: '#/components/parameters/ChannelId'
        - $ref: '#/components/parameters/MessageId'
      responses:
        '204':
          description: Fixação removida
        '400':
          description: Parâmetro ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /channels/{channel_id}/pinned:
    get:
      tags: [Messages]
      summary: Lista as mensagens pinadas do canal
      description: "Permission Check: `read_channel` do canal. Retorna as mensagens pinadas com attachments, previews, thumbnails e reações, na ordem em que foram fixadas (pinned_at crescente)."
      parameters:
        - $ref: '#/components/parameters/ChannelId'
      responses:
        '200':
          description: Mensagens pinadas
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PinnedMessageList'
        '400':
          description: Parâmetro ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /channels/{channel_id}/messages/{message_id}/reactions:
    post:
      tags: [Messages]
      summary: Adiciona reação do usuário autenticado à mensagem
      description: "Permission Check: `send_messages` do canal. Corpo: exatamente um de `emoji_id` (emoji custom do banco) ou `unicode` (emoji unicode, máx 16 caracteres). Idempotente: reagir de novo com o mesmo emoji retorna 200. Limite de 20 tipos de reação por mensagem."
      parameters:
        - $ref: '#/components/parameters/ChannelId'
        - $ref: '#/components/parameters/MessageId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReactionRequest'
      responses:
        '200':
          description: Usuário já havia reagido com este emoji
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageReaction'
        '201':
          description: Reação adicionada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageReaction'
        '400':
          description: Ambos ou nenhum de emoji_id/unicode informados, ou unicode inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Mensagem não pertence ao canal, canal não existe ou emoji não existe
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '409':
          description: Limite de 20 tipos de reação por mensagem atingido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'
    get:
      tags: [Messages]
      summary: Lista reações da mensagem com os usuários que reagiram (Limite de 100 por requisição, use cursor com since + last_id para paginar)
      description: "Permission Check: `read_channel` do canal. Resposta agrupada por tipo de emoji, em ordem decrescente de criação (created_at, id). O cursor da próxima página é o par (created_at, id) da última reação da página (cada usuário do grupo carrega os dois valores). `count` é o número de usuários do grupo na página retornada; o total por emoji está em `reactions` do corpo da mensagem."
      parameters:
        - $ref: '#/components/parameters/ChannelId'
        - $ref: '#/components/parameters/MessageId'
        - name: since
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Timestamp ISO 8601 (criado após)
        - name: last_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: ID da última reação da página anterior (cursor exato usado com since)
      responses:
        '200':
          description: Reações da mensagem
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageReactionList'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Mensagem não pertence ao canal ou canal não existe
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'
    delete:
      tags: [Messages]
      summary: Remove a própria reação do usuário autenticado da mensagem
      description: "Permission Check: `read_channel` do canal. Corpo: exatamente um de `emoji_id` ou `unicode`."
      parameters:
        - $ref: '#/components/parameters/ChannelId'
        - $ref: '#/components/parameters/MessageId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReactionRequest'
      responses:
        '204':
          description: Reação removida
        '400':
          description: Ambos ou nenhum de emoji_id/unicode informados, ou unicode inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Mensagem não pertence ao canal, canal não existe, emoji não existe ou usuário não reagiu com este emoji
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'

  /emojis:
    get:
      tags: [Emojis]
      summary: Lista emojis (Limite de 25 por requisição, use cursor com since + last_id para paginar)
      parameters:
        - name: since
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Timestamp ISO 8601 (paginação por created_at)
        - name: last_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: ID do último emoji da página anterior (cursor exato usado com since)
      responses:
        '200':
          description: Lista de emojis
          content:
            application/json:
              schema:
                type: object
                properties:
                  emojis:
                    type: array
                    items:
                      $ref: '#/components/schemas/Emoji'
                  has_more:
                    type: boolean
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'
    post:
      tags: [Emojis]
      summary: Cria emoji (GIF/JPEG/JPG/PNG/WEBP, máx. 256kb, 512px)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEmojiRequest'
      responses:
        '201':
          description: Emoji criado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Emoji'
        '400':
          description: Campo ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '409':
          description: Nome de emoji já existe ou limite de emojis atingido (500)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '500':
          $ref: '#/components/responses/InternalError'
  /emojis/{emoji_id}:
    delete:
      tags: [Emojis]
      summary: Remove emoji
      description: "Permission Check: Author ou `manage_server`"
      parameters:
        - $ref: '#/components/parameters/EmojiId'
      responses:
        '204':
          description: Emoji removido
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /attachments/{file_id}:
    get:
      tags: [Attachments]
      summary: Download de arquivo
      description: "Permission Check: `read_channel` do canal da mensagem que possui o attachment"
      parameters:
        - name: file_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Arquivo binário (Content-Type = mime type do arquivo)
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '206':
          description: "Parte do arquivo (Range request; Content-Range indica o intervalo atendido)"
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '416':
          description: Intervalo fora do alcance do arquivo (Range não satisfatível)
        '500':
          $ref: '#/components/responses/InternalError'

  /attachments/{file_id}/thumbnail:
    get:
      tags: [Attachments]
      summary: Download da thumbnail do arquivo
      description: >
        Thumbnail gerada no upload (WebP para imagens estáticas, GIF para
        GIFs animados). Permission Check: `read_channel` do canal da mensagem
        que possui o attachment (mesmo check do download original).
      parameters:
        - name: file_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: "Thumbnail binária (Content-Type = image/webp ou image/gif, Content-Disposition: inline)"
          content:
            image/webp:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /media/{sha_hash}:
    get:
      tags: [Media]
      summary: Download de mídia content-addressable (banner, avatar, ícone, ...)
      description: "O sha_hash é unguessable (sha256 do conteúdo); autenticação suficiente, sem check de permissão"
      parameters:
        - name: sha_hash
          in: path
          required: true
          schema:
            type: string
            description: sha256 (hex) da mídia
      responses:
        '200':
          description: "Arquivo binário (Content-Type = mime type da mídia, Content-Disposition: inline)"
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '206':
          description: "Parte do arquivo (Range request; Content-Range indica o intervalo atendido)"
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '416':
          description: Intervalo fora do alcance do arquivo (Range não satisfatível)
        '500':
          $ref: '#/components/responses/InternalError'

  /link-previews/{preview_id}:
    get:
      tags: [LinkPreviews]
      summary: Link preview (dados + imagem em base64)
      description: >
        Dados do link preview com a imagem embutida em base64 (image_data).
        Permission Check: `read_channel` do canal da mensagem à qual o preview
        está vinculado (preview sem vínculo acessível → 404). Preview sem
        imagem é acessível (image_data nulo).
      parameters:
        - name: preview_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Preview em JSON com image_data (base64) quando existe
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LinkPreviewWithImage'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '500':
          $ref: '#/components/responses/InternalError'

  /search:
    post:
      tags: [Search]
      summary: Busca com diversos parâmetros (Limite de 100 por requisição, use cursor com since + last_id para paginar)
      description: |
        Os resultados são limitados a canais legíveis pelo usuário autenticado
        (dono do servidor, canais abertos sem permissões definidas ou canais
        em que uma das suas roles tem read_channel).
      parameters:
        - name: since
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Timestamp ISO 8601 (paginação por created_at)
        - name: last_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: ID do último resultado da página anterior (cursor exato usado com since)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchRequest'
      responses:
        '200':
          description: Resultados da busca
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
        '400':
          description: Termo de busca ausente ou inválido
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '500':
          $ref: '#/components/responses/InternalError'

  /admin/audit-logs:
    get:
      tags: [Admin]
      summary: Lista os logs de auditoria (Limite de 100 por requisição, use cursor com since + last_id para paginar)
      description: |
        Retorna os eventos auditáveis em ordem decrescente (mais recentes
        primeiro). Requer a permissão `manage_server` (dono do servidor ou role
        com a permissão). Os filtros opcionais podem ser combinados.
      parameters:
        - name: action
          in: query
          required: false
          schema:
            type: string
          description: 'Filtra por ação (ex.: user.ban, message.delete, role.create)'
        - name: actor_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Filtra por quem executou a ação
        - name: entity_type
          in: query
          required: false
          schema:
            type: string
          description: Filtra por tipo de entidade (user, server, channel, message, attachment, role, user_role, emoji)
        - name: since
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Timestamp ISO 8601 (created_at >= since)
        - name: until
          in: query
          required: false
          schema:
            type: string
            format: date-time
          description: Timestamp ISO 8601 (created_at <= until)
        - name: last_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: ID do último log da página anterior (cursor exato usado com since)
      responses:
        '200':
          description: Logs de auditoria
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLogList'
        '400':
          description: Parâmetro inválido (since/until fora do formato ISO 8601)
          content:
            application/problem+json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '500':
          $ref: '#/components/responses/InternalError'

  /ws:
    get:
      tags: [WebSocket]
      summary: WebSocket (handshake com o mesmo cookie Auth)
      description: |
        Endpoint WebSocket: `ws://localhost:8080/ws`. O JWT do cookie `Auth`
        é validado no handshake. Eventos (conforme `websocket/events.go` e `webrtc/events.go`):

        
        Semântica de conexão dos eventos de voz:
        `voice_join` vincula o Peer WebRTC à conexão WebSocket que enviou o evento.
        Os eventos de sinalização e controle dessa call devem ser enviados pela mesma conexão WebSocket.
        `voice_answer`, `voice_offer` outbound, `voice_ice_candidate` outbound e erros de sinalização são enviados somente para a conexão dona da call, não para todas as abas/dispositivos do mesmo usuário.
        Se a conexão WebSocket dona da call fechar, o Peer é removido mesmo que o mesmo usuário possua outra conexão WebSocket aberta.

        | Event            | Direction | Payload                                                                           |
        | ---------        | --------- | ---------------------------------------------------------------------             |
        | `message`        | outbound  | `{ type: "message", id, channel_id, author_id, content, reply_to?, attachments?, created_at }` (reply_to: null quando não é resposta; sem reactions/previews/user_reactions) |
        | `message_edit`   | outbound  | `{ type: "message_edit", id, channel_id, content, edited_at }`                          |
        | `message_delete` | outbound  | `{ type: "message_delete", id, channel_id }`                                            |
        | `attachment_moderation_update` | outbound  | `{ type: "attachment_moderation_update", channel_id, message_id, attachment_id, status }` (status: sensitive; o blocked exclui a mensagem e chega como `message_delete`) |
        | `message_pin`    | outbound  | `{ type: "message_pin", message_id, is_pinned }` (is_pinned: true quando a mensagem foi pinada, false quando a fixação foi removida) |
        | `react_update`   | outbound  | `{ type: "react_update", message_id, emoji_id, unicode, count }` (emoji_id/unicode: um é null; count = usuários que reagiram com aquele emoji, 0 quando a última foi removida) |
        | `link_preview_update` | outbound | `{ type: "link_preview_update", channel_id, message_id, preview }` (preview: objeto completo do link preview, mesma forma de `GET /link-previews/{preview_id}` incluindo `image_data`; um evento por mensagem vinculada, distribuído aos leitores do canal quando o preview é refetchado com cache expirada) |
        | `new_preview`    | outbound  | `{ type: "new_preview", message_id, preview_id }` (sem channel_id; distribuído após o processamento em background (crawl) da mensagem; um evento por preview) |
        | `remove_preview` | outbound  | `{ type: "remove_preview", message_id, preview_id }` (sem channel_id; edição que substitui/limpa os previews; um evento por preview) |
        | `new_notification` | unicast  | `{ type: "new_notification", id, user_id, message_id, message_content }` (id: row de notificação ou UUID efêmero quando a configuração all gera evento sem row; user_id: autor da mensagem; message_content: truncado a 512 caracteres) |
        | `channel_create` | outbound  | `{ type: "channel_create", channel_id, name, channel_type, position, topic? }` (topic: null para canais category) |
        | `channel_update` | outbound  | `{ type: "channel_update", channel_id, name, position, topic? }`                               |
        | `channel_delete` | outbound  | `{ type: "channel_delete", channel_id }`                                               |
        | `typing`         | inbound   | `{ type: "typing", channel_id }`                                                        |
        | `typing`         | outbound  | `{ type: "typing", channel_id, user_id, is_typing }`                                    |
        | `avatar_update`  | outbound  | `{ type: "avatar_update", user_id }`                                                    |
        | `user_join`      | outbound  | `{ type: "user_join", user_id }`                                                        |
        | `role_add`       | outbound  | `{ type: "role_add", user_id, role_id }` (role atribuída ao usuário)                     |
        | `role_remove`    | outbound  | `{ type: "role_remove", user_id, role_id }` (role removida do usuário)                   |
        | `presence_update`| outbound  | `{ type: "presence_update", user_id, status, status_message, typing, nickname }` (status: online/offline/away/busy) |
        | `presence_sync`  | unicast   | `{ type: "presence_sync", members: (user_id, status, status_message)[] }` (status: online/offline/away/busy; enviado na conexão; sem nickname) |
        | `heartbeat`      | inbound   | `{ type: "heartbeat" }`                                                                 |
        | `heartbeat_ack`  | outbound  | `{ type: "heartbeat_ack" }`                                                             |
        | `error`          | outbound  | `{ type: "error", message, code? }`                                                     |
        | `voice_join`     | inbound   | `{ type: "voice_join", channel_id }`                                                    |
        | `voice_leave` | outbound | `{ type: "voice_leave", channel_id, user_id }` (saída explícita, conexão WebSocket dona da call caiu, sessão revogada ou sala destruída) |
        | `voice_offer`    | inbound   | `{ type: "voice_offer", channel_id, sdp }` (client-offers-first; renegociações do servidor chegam como `voice_offer` outbound) |
        | `voice_answer`   | inbound   | `{ type: "voice_answer", channel_id, sdp }`                                             |
        | `voice_ice_candidate` | inbound | `{ type: "voice_ice_candidate", channel_id, candidate, sdp_mid?, sdp_mline_index? }`    |
        | `track_subscribe`   | inbound | `{ type: "track_subscribe", channel_id, publisher_id, kind }` (kind: video/screen; vídeo só é forwardado com subscribe explícito) |
        | `track_unsubscribe` | inbound | `{ type: "track_unsubscribe", channel_id, publisher_id, kind }` (kind: video/screen) |
        | `voice_mute` | inbound | `{ type: "voice_mute", channel_id, muted }` (mute autoritativo do servidor: usuário mutado não entra no active-speaker e seu áudio não é forwardado pelo SFU) |
        | `voice_camera` | inbound | `{ type: "voice_camera", channel_id, on }` (deve ser enviado antes do `voice_offer` correspondente; o backend usa esse estado para identificar/validar a publicação de câmera) |
        | `screen_share_start` | inbound | `{ type: "screen_share_start", channel_id }` (deve ser enviado antes do `voice_offer` que adiciona/publica a track de screen) |
        | `screen_share_stop` | inbound | `{ type: "screen_share_stop", channel_id }` (deve ser enviado antes do `voice_offer` que remove/inativa a publicação de screen) |
        | `voice_joined`   | unicast   | `{ type: "voice_joined", channel_id, members: (user_id, muted, camera_on, screen_sharing)[], active_speakers: user_id[] }` (estado inicial do late joiner, o peer novo começa com muted=true camera_on=false screen_sharing=false) |
        | `voice_answer`   | unicast   | `{ type: "voice_answer", channel_id, sdp }` (resposta ao `voice_offer` do cliente)      |
        | `voice_offer`    | unicast   | `{ type: "voice_offer", channel_id, sdp }` (renegociação iniciada pelo servidor)        |
        | `voice_ice_candidate` | unicast | `{ type: "voice_ice_candidate", channel_id, candidate, sdp_mid?, sdp_mline_index? }`    |
        | `voice_state_update` | outbound | `{ type: "voice_state_update", channel_id, user_id, muted, camera_on, screen_sharing }` |
        | `voice_leave`    | outbound  | `{ type: "voice_leave", channel_id, user_id }` (explícita, WS caiu, sessão revogada ou sala destruída) |
        | `active_speaker_update` | outbound | `{ type: "active_speaker_update", channel_id, user_ids: user_id[] }` (top-K active speakers) |
      responses:
        '101':
          description: WebSocket upgrade
        '401':
          $ref: '#/components/responses/Unauthorized'

components:
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: Auth
      description: JWT em cookie HttpOnly (Path=/, Secure, SameSite=Strict)

  parameters:
    UserId:
      name: user_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    ChannelId:
      name: channel_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    RoleId:
      name: role_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    MessageId:
      name: message_id
      in: path
      required: true
      schema:
        type: string
        format: uuid
    EmojiId:
      name: emoji_id
      in: path
      required: true
      schema:
        type: string
        format: uuid

  responses:
    Unauthorized:
      description: Token ausente, inválido ou expirado
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Error'
    Forbidden:
      description: Autenticado, mas sem permissão
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Error'
    NotFound:
      description: Recurso não encontrado
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Error'
    Banned:
      description: IP/usuário banido
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Error'
    InternalError:
      description: Erro interno do servidor
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Error'

  schemas:
    Error:
      type: object
      description: Erro RFC 7807 (application/problem+json)
      required: [type, title, status]
      properties:
        type:
          type: string
          example: https://papo.com/errors/invalid-param
        title:
          type: string
          example: Parâmetro inválido
        status:
          type: integer
          example: 400
        detail:
          type: string
          example: Campo 'username' é obrigatório
        instance:
          type: string
          example: /auth/login

    GenericResponse:
      type: object
      properties:
        response:
          type: string
          example: User status updated successfully

    RegisterRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
        password:
          type: string
          description: Precisa ter no mínimo 8 caracteres, 1 letra maíscula + 1 caractere especial

    RegisterResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        created_at:
          type: string
          format: date-time

    LoginRequest:
      type: object
      required: [username, password]
      properties:
        username:
          type: string
        password:
          type: string

    LoginResponse:
      type: object
      description: O JWT não é retornado no corpo; a sessão é entregue exclusivamente pelo cookie Auth (HttpOnly)
      properties:
        user:
          type: object
          properties:
            id:
              type: string
              format: uuid
            username:
              type: string
        connection_violation:
          type: boolean
          description: Verdadeiro se houve detecção de reuso de token de sessão (todas as conexões foram revogadas)

    LoginServerRequest:
      type: object
      required: [server_password]
      properties:
        server_password:
          type: string

    UserSummary:
      type: object
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        nickname:
          type: string
          nullable: true
        status:
          type: string
          nullable: true
          enum: [away, busy]
          description: Status persistido do usuário (vale enquanto online)
        status_message:
          type: string
          nullable: true
        typing:
          type: string
          nullable: true
          maxLength: 64
          description: Frase de digitação personalizada (exibida no cliente quando o usuário está digitando)
        status_updated_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        roles:
          type: array
          description: Roles atribuídas ao usuário
          items:
            $ref: '#/components/schemas/RoleSummary'

    UserProfile:
      type: object
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        nickname:
          type: string
          nullable: true
        avatar_blob:
          type: string
          format: byte
          description: Avatar em base64
        avatar_format:
          type: string
          example: PNG
        banner_media:
          type: string
          nullable: true
          description: Referência (sha256) do banner na mídia content-addressable; o arquivo é baixado via GET /media/{sha_hash}
        description:
          type: string
          nullable: true
          maxLength: 512
        status:
          type: string
          nullable: true
          enum: [away, busy]
          description: Status persistido do usuário (vale enquanto online)
        status_message:
          type: string
          nullable: true
        typing:
          type: string
          nullable: true
          maxLength: 64
          description: Frase de digitação personalizada (exibida no cliente quando o usuário está digitando)
        status_updated_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        roles:
          type: array
          description: Roles atribuídas ao usuário
          items:
            $ref: '#/components/schemas/RoleSummary'

    ProfileBatchRequest:
      type: object
      required: [ids]
      properties:
        ids:
          type: array
          minItems: 1
          maxItems: 50
          items:
            type: string
            format: uuid

    WhoamiResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        username:
          type: string
        nickname:
          type: string
          nullable: true
        avatar_blob:
          type: string
          format: byte
          description: Avatar em base64
        avatar_format:
          type: string
          example: PNG
        status:
          type: string
          nullable: true
          enum: [away, busy]
          description: Status persistido do usuário (vale enquanto online)
        status_message:
          type: string
          nullable: true
        typing:
          type: string
          nullable: true
          maxLength: 64
          description: Frase de digitação personalizada (exibida no cliente quando o usuário está digitando)
        status_updated_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        roles:
          type: array
          description: Roles atribuídas ao usuário
          items:
            $ref: '#/components/schemas/RoleSummary'
        settings:
          type: object
          properties:
            version:
              type: integer
            config:
              $ref: '#/components/schemas/UserConfig'
        connection_violation:
          type: boolean
          description: Verdadeiro se houve detecção de reuso de token de sessão (todas as conexões foram revogadas)

    Connection:
      type: object
      properties:
        id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
          description: Expiração do JWT desta conexão (token_issued_at + 24h)

    RefreshResponse:
      type: object
      description: O novo JWT não é retornado no corpo; é entregue exclusivamente pelo cookie Auth (HttpOnly)
      properties:
        connection:
          $ref: '#/components/schemas/Connection'

    ConnectedDevicesResponse:
      type: object
      properties:
        connections:
          type: array
          items:
            $ref: '#/components/schemas/Connection'

    DropConnectionRequest:
      type: object
      required: [connection_id]
      properties:
        connection_id:
          type: string
          description: UUID da conexão a revogar, ou "ALL" para revogar todas

    DropConnectionResponse:
      type: object
      properties:
        dropped:
          type: integer
          description: Número de conexões revogadas

    UserSettings:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        version:
          type: integer
        config:
          $ref: '#/components/schemas/UserConfig'
        updated_at:
          type: string
          format: date-time

    UserConfig:
      type: object
      properties:
        theme:
          type: string
          enum: [dark, light, system]
        notifications:
          $ref: '#/components/schemas/Notifications'
        display:
          $ref: '#/components/schemas/Display'

    Notifications:
      type: object
      properties:
        enabled:
          type: boolean
        messagePreview:
          type: boolean
        sound:
          type: boolean
        mentions:
          type: boolean

    Display:
      type: object
      properties:
        fontSize:
          type: string
          enum: [small, medium, huge]
        messageDensity:
          type: string
          enum: [compact, normal, comfortable]
        showTimestamps:
          type: boolean
        showAvatars:
          type: boolean

    UpdateUserRequest:
      type: object
      required: [nickname, status, description]
      properties:
        nickname:
          type: string
          maxLength: 32
        status:
          type: string
          maxLength: 64
        description:
          type: string
          maxLength: 512
        typing:
          type: string
          nullable: true
          maxLength: 64
          description: Frase de digitação personalizada; ausente (null) não altera o valor atual

    UpdateAvatarRequest:
      type: object
      required: [avatar, avatar_format]
      properties:
        avatar:
          type: string
          description: Base64 de GIF, JPEG/JPG, PNG ou WEBP (máx. 2MB, 512px)
        avatar_format:
          type: string
          enum: [GIF, JPEG, JPG, PNG, WEBP]

    UpdateBannerRequest:
      type: object
      required: [banner, banner_format]
      properties:
        banner:
          type: string
          description: Base64 de GIF, JPEG/JPG, PNG ou WEBP (máx. 2MB, 2048px); vazio remove o banner
        banner_format:
          type: string
          enum: [GIF, JPEG, JPG, PNG, WEBP]
          description: Vazio remove o banner

    UpdateStatusRequest:
      type: object
      properties:
        status:
          type: string
          nullable: true
          enum: [away, busy]
          description: Status persistido (away/busy); null remove o status

    ChangePasswordRequest:
      type: object
      required: [password]
      properties:
        password:
          type: string
          description: Nova senha do usuário, precisa ter no mínimo 8 caracteres, 1 letra maíscula + 1 caractere especial

    BanUserRequest:
      type: object
      required: [ban_state]
      properties:
        user_id:
          type: string
          format: uuid
          description: ID do usuário (o id da URL é o autoritativo)
        ban_state:
          type: boolean
          description: true para banir, false para desbanir

    UpdateServerRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        icon_blob:
          type: string
          format: byte
          description: Ícone em base64 (GIF/JPEG/JPG/PNG/WEBP, máx. 2MB, 512px); vazio remove o ícone
        icon_format:
          type: string
          enum: [GIF, JPEG, JPG, PNG, WEBP]
        password:
          type: string
          description: Senha do servidor precisa ter no mínimo 8 caracteres, 1 letra maíscula + 1 caractere especial; obrigatória quando o servidor ficar privado (public=false)
        public:
          type: boolean
          description: Servidor público; ausente mantém a visibilidade atual

    CreateServerRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        icon_blob:
          type: string
          format: byte
          description: Ícone em base64 (GIF/JPEG/JPG/PNG/WEBP, máx. 2MB, 512px)
        icon_format:
          type: string
          enum: [GIF, JPEG, JPG, PNG, WEBP]
        password:
          type: string
          description: Senha do servidor, precisa ter no mínimo 8 caracteres, 1 letra maíscula + 1 caractere especial; obrigatória quando public=false
        public:
          type: boolean
          description: Servidor público; ausente significa público (default do schema)

    CreateServerResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        icon_blob:
          type: string
          format: byte
        icon_format:
          type: string
        owner_id:
          type: string
          format: uuid
        owner_username:
          type: string
        created_at:
          type: string
          format: date-time

    Server:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        icon_blob:
          type: string
          format: byte
          description: Ícone em base64
        icon_format:
          type: string
          example: PNG
        owner_id:
          type: string
          format: uuid
        public:
          type: boolean
          description: Servidor público
        owner_username:
          type: string
        created_at:
          type: string
          format: date-time
        channel_count:
          type: integer
        member_count:
          type: integer
        role_count:
          type: integer

    Channel:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        type:
          type: string
          enum: [text, category, voice]
        position:
          type: integer
          description: Posição do canal na visualização (1-based)
        topic:
          type: string
          nullable: true
          description: >
            Tópico do canal (máx 512 caracteres). Válido apenas para canais de
            texto ou voz; null para canais category ou sem tópico.
        permissions:
          type: array
          items:
            $ref: '#/components/schemas/ChannelPermissionEntry'
        created_at:
          type: string
          format: date-time
        last_message:
          $ref: '#/components/schemas/ChannelLastMessage'
        last_read_message:
          type: string
          format: uuid
          nullable: true
          description: >
            Última mensagem lida pelo usuário autenticado neste canal
            (user_channel_state). Null quando o usuário ainda não leu o canal.
        last_read_at:
          type: string
          format: date-time
          nullable: true
          description: >
            Momento em que o usuário autenticado leu até last_read_message.
            Null quando o usuário ainda não leu o canal.
        notification_settings:
          $ref: '#/components/schemas/NotificationSettings'
    NotificationSettings:
      type: string
      enum: [off, only_mentions, all]
      description: >
       Configuração de notificação do usuário autenticado neste canal (channel_user_settings). Ausência de row equivale ao padrão only_mentions.
        off: nenhuma notificação; only_mentions: notifica apenas quando o
        usuário é mencionado diretamente, é o autor da mensagem respondida
        (reply_to) ou o canal recebe @everyone; all: notifica toda mensagem
        do canal.
        

    ChannelUserSetting:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        channel_id:
          type: string
          format: uuid
        notification_settings:
          $ref: '#/components/schemas/NotificationSettings'
        updated_at:
          type: string
          format: date-time

    UpdateChannelUserSettingRequest:
      type: object
      required: [notification_settings]
      properties:
        notification_settings:
          $ref: '#/components/schemas/NotificationSettings'

    Notification:
      type: object
      properties:
        id:
          type: string
          format: uuid
        message_id:
          type: string
          format: uuid
        channel_id:
          type: string
          format: uuid
        author_id:
          type: string
          format: uuid
          nullable: true
          description: Autor da mensagem notificada (null quando o autor foi removido)
        message_content:
          type: string
          description: Conteúdo da mensagem notificada, truncado a 512 caracteres
        read:
          type: boolean
        created_at:
          type: string
          format: date-time

    NotificationList:
      type: object
      properties:
        notifications:
          type: array
          items:
            $ref: '#/components/schemas/Notification'
        has_more:
          type: boolean

    MarkNotificationsReadRequest:
      type: object
      required: [notification_ids]
      properties:
        notification_ids:
          type: array
          minItems: 1
          maxItems: 1000
          items:
            type: string
            format: uuid

    MarkNotificationsReadResponse:
      type: object
      properties:
        updated:
          type: integer
          description: Número de notificações marcadas como lidas

    ChannelPermissionEntry:
      type: object
      properties:
        role_id:
          type: string
          format: uuid
        role_name:
          type: string
        permissions:
          $ref: '#/components/schemas/ChannelPermissions'

    ChannelPermissions:
      type: object
      properties:
        read_channel:
          type: boolean
        send_messages:
          type: boolean
        delete_messages:
          type: boolean
        connect_voice:
          type: boolean
          description: Permite entrar em calls do canal (padrão false)

    ChannelPermissionsResponse:
      type: object
      properties:
        channel_id:
          type: string
          format: uuid
        permissions:
          type: array
          items:
            $ref: '#/components/schemas/ChannelPermissionEntry'

    ICEServer:
      type: object
      properties:
        urls:
          type: array
          items:
            type: string
        username:
          type: string
          nullable: true
        credential:
          type: string
          nullable: true

    ICEServersResponse:
      type: object
      properties:
        ice_servers:
          type: array
          items:
            $ref: '#/components/schemas/ICEServer'

    CreateChannelRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        type:
          type: string
          enum: [text, category, voice]
          description: Tipo do canal (padrão text)
        topic:
          type: string
          description: >
            Tópico do canal (máx 512 caracteres). Opcional; válido apenas para
            canais de texto ou voz.

    UpdateChannelRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        topic:
          type: string
          nullable: true
          description: >
            Tópico do canal (máx 512 caracteres). Opcional; ausente não altera
            o tópico, string vazia limpa o tópico. Válido apenas para canais de
            texto ou voz.

    ChangeChannelPositionRequest:
      type: object
      required: [old_position, new_position]
      properties:
        old_position:
          type: integer
        new_position:
          type: integer

    UpdateChannelPermissionsRequest:
      type: object
      required: [permissions]
      properties:
        permissions:
          $ref: '#/components/schemas/ChannelPermissions'

    Message:
      type: object
      nullable: true
      properties:
        id:
          type: string
          format: uuid
        channel_id:
          type: string
          format: uuid
        author_id:
          type: string
          format: uuid
        content:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        edited_at:
          type: string
          format: date-time
          nullable: true
        reply_to:
          type: string
          format: uuid
          nullable: true
          description: >
            Id da mensagem respondida (sempre do mesmo canal). Null quando a
            mensagem não é uma resposta. Sem FK: pode apontar para uma mensagem
            já excluída (o cliente exibe "mensagem não disponível").
        attachments:
          type: array
          items:
            $ref: '#/components/schemas/Attachment'
          description: >
            O schema (attachments.messages_id) permite múltiplos attachments
            por mensagem.
        previews:
          type: array
          items:
            $ref: '#/components/schemas/LinkPreview'
          description: >
            Link previews (OpenGraph/oEmbed) extraídos do content da mensagem
            (best-effort; vazio quando não há URL ou a extração falhou).
        reactions:
          type: array
          items:
            $ref: '#/components/schemas/MessageReactionSummary'
          description: >
            Contagem de tipos de reação da mensagem (sem a lista de usuários;
            use GET /channels/{channel_id}/messages/{message_id}/reactions).
            Null quando a mensagem não tem reações.
        user_reactions:
          type: array
          items:
            $ref: '#/components/schemas/MessageUserReaction'
          description: >
            Reações do usuário autenticado à mensagem (indica com quais emojis
            ele já reagiu). Vazio quando o usuário não reagiu.

    ChannelLastMessage:
      type: object
      nullable: true
      description: >
        Última mensagem do canal em forma resumida, como exibida na listagem
        de canais (diferente do schema Message): inclui author_username e não
        inclui channel_id, edited_at, reply_to, attachments, previews,
        reactions ou user_reactions.
      properties:
        id:
          type: string
          format: uuid
        content:
          type: string
          nullable: true
        author_id:
          type: string
          format: uuid
          nullable: true
        author_username:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time

    PinnedMessage:
      type: object
      properties:
        channel_id:
          type: string
          format: uuid
        message_id:
          type: string
          format: uuid
        pinned_by:
          type: string
          format: uuid
          nullable: true
          description: >
            Usuário que fixou a mensagem. Null quando o usuário foi excluído.
        pinned_at:
          type: string
          format: date-time

    PinnedMessageList:
      type: object
      properties:
        channel_id:
          type: string
          format: uuid
        pinned:
          type: array
          items:
            $ref: '#/components/schemas/Message'
          description: >
            Mensagens pinadas do canal, na ordem em que foram fixadas
            (pinned_at crescente). Vazio quando o canal não tem mensagens
            pinadas.

    MessageListResponse:
      type: object
      properties:
        channel_id:
          type: string
          format: uuid
        messages:
          type: array
          items:
            $ref: '#/components/schemas/Message'
        has_more:
          type: boolean

    ReactionRequest:
      type: object
      properties:
        emoji_id:
          type: string
          format: uuid
          nullable: true
          description: >
            Id de um emoji custom do banco. Exatamente um de emoji_id ou
            unicode deve ser informado.
        unicode:
          type: string
          nullable: true
          description: >
            Emoji unicode (máx 16 caracteres). Exatamente um de emoji_id ou
            unicode deve ser informado.

    MessageReaction:
      type: object
      properties:
        message_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        emoji_id:
          type: string
          format: uuid
          nullable: true
          description: Emoji custom do banco (null para emoji unicode)
        unicode:
          type: string
          nullable: true
          description: Emoji unicode (null para emoji custom)
        created_at:
          type: string
          format: date-time

    MessageReactionSummary:
      type: object
      properties:
        emoji_id:
          type: string
          format: uuid
          nullable: true
          description: Emoji custom do banco (null para emoji unicode)
        unicode:
          type: string
          nullable: true
          description: Emoji unicode (null para emoji custom)
        count:
          type: integer
          description: Número de usuários que reagiram com este emoji

    MessageReactionUser:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: >
            Id da reação (cursor de paginação: use com created_at como
            since + last_id).
        user_id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
          description: Criado da reação (metade do cursor de paginação)

    MessageReactionGroup:
      type: object
      properties:
        emoji_id:
          type: string
          format: uuid
          nullable: true
          description: Emoji custom do banco (null para emoji unicode)
        unicode:
          type: string
          nullable: true
          description: Emoji unicode (null para emoji custom)
        count:
          type: integer
          description: >
            Número de usuários que reagiram com este emoji NA PÁGINA
            RETORNADA (o total do emoji está em `reactions` do corpo da
            mensagem).
        users:
          type: array
          items:
            $ref: '#/components/schemas/MessageReactionUser'
          description: >
            Usuários que reagiram com este emoji na página, em ordem
            decrescente de criação.

    MessageReactionList:
      type: object
      properties:
        message_id:
          type: string
          format: uuid
        reactions:
          type: array
          items:
            $ref: '#/components/schemas/MessageReactionGroup'
          description: >
            Agrupadas por tipo de emoji; um grupo pode continuar na página
            seguinte (o cliente deve mesclar grupos com o mesmo
            emoji_id/unicode).
        has_more:
          type: boolean
          description: Indica se existe próxima página (cursor since + last_id)

    MessageUserReaction:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Id da reação do usuário autenticado à mensagem
        emoji_id:
          type: string
          format: uuid
          nullable: true
          description: Emoji custom do banco (null para emoji unicode)
        unicode:
          type: string
          nullable: true
          description: Emoji unicode (null para emoji custom)

    CreateMessageRequest:
      type: object
      required: [channel_id]
      properties:
        channel_id:
          type: string
          format: uuid
        content:
          type: string
        reply_to:
          type: string
          format: uuid
          description: >
            Opcional. Id da mensagem respondida; deve existir e estar no mesmo
            canal.
        attachments:
          type: array
          items:
            type: string
            format: binary

    PushRegistrationRequest:
      type: object
      required: [provider, address, auth_token, platform]
      properties:
        provider:
          type: string
          enum: [papo_relay]
        address:
          type: string
          format: uri
          writeOnly: true
          description: >
            HTTPS capability endpoint emitido por push.papo.chat. Não é um
            token FCM e nunca é retornado pelo backend.
        auth_token:
          type: string
          writeOnly: true
          description: Bearer secret da capability; nunca retornado nem logado.
        platform:
          type: string
          enum: [android]
        app_version:
          type: string
          nullable: true

    PushRegistration:
      type: object
      properties:
        id:
          type: string
          format: uuid
        installation_id:
          type: string
          format: uuid
        provider:
          type: string
          enum: [papo_relay]
        platform:
          type: string
          enum: [android]
        app_version:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    UpdateMessageRequest:
      type: object
      required: [content]
      properties:
        content:
          type: string

    Attachment:
      type: object
      description: >
        Informação mínima do attachment exposta nas respostas de mensagens
        (listagem, criação e edição).
      properties:
        id:
          type: string
          format: uuid
        mime_type:
          type: string
        original_file_name:
          type: string
        size_bytes:
          type: integer
          format: int64
        thumbnail_id:
          type: string
          format: uuid
          nullable: true
          description: >
            ID da thumbnail gerada no upload (imagens jpeg/png/webp/gif);
            null quando não há thumbnail. Download em
            /attachments/{file_id}/thumbnail.
        created_at:
          type: string
          format: date-time
        moderation_status:
          type: string
          description: >
            Estado da moderação assíncrona de imagens (nudez/gore):
            pending/processing/clean/sensitive/blocked/failed
            (pending quando a moderação está desativada).

    LinkPreview:
      type: object
      description: >
        Preview de link (OpenGraph ou oEmbed) em cache por URL normalizada.
        kind: 'og' (OpenGraph) ou 'oembed' (provedor allowlistado).
      properties:
        id:
          type: string
          format: uuid
        url:
          type: string
          format: uri
          description: URL normalizada (chave do cache)
        kind:
          type: string
          enum: [og, oembed]
        title:
          type: string
          nullable: true
          description: Título (máx. 200 chars, truncado)
        description:
          type: string
          nullable: true
          description: Descrição (máx. 300 chars, truncada)
        provider_name:
          type: string
          nullable: true
          description: Nome do provedor (oEmbed)
        embed_url:
          type: string
          nullable: true
          description: >
            Embed de vídeo derivado de padrão hardcoded (MVP: apenas
            YouTube, formato https://www.youtube.com/embed/<id>). O frontend
            deve renderizar iframe somente se casar exatamente com esse
            padrão.
        image_mime_type:
          type: string
          nullable: true
          description: MIME da thumbnail da imagem do preview (webp/gif)
        image_size_bytes:
          type: integer
          format: int64
          nullable: true
        fetched_at:
          type: string
          format: date-time

    LinkPreviewWithImage:
      description: >
        LinkPreview + imagem embutida em base64 (resposta de
        GET /link-previews/{preview_id}).
      allOf:
        - $ref: '#/components/schemas/LinkPreview'
        - type: object
          properties:
            image_data:
              type: string
              format: byte
              nullable: true
              description: Imagem do preview em base64 (null quando não há imagem)

    Role:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        color:
          type: string
          nullable: true
          example: '#FF0000'
        permissions:
          $ref: '#/components/schemas/RolePermissions'
        created_at:
          type: string
          format: date-time

    RoleSummary:
      type: object
      description: Visão reduzida de role exposta nas respostas de usuário (whoami, profile, listagem de usuários)
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        color:
          type: string
          nullable: true
          example: '#FF0000'

    RolePermissions:
      type: object
      properties:
        manage_server:
          type: boolean
        manage_channels:
          type: boolean
        manage_roles:
          type: boolean
        ban_members:
          type: boolean
        pin_message:
          type: boolean
        everyone_message:
          type: boolean
        send_attachment:
          type: boolean

    CreateRoleRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        color:
          type: string
        permissions:
          $ref: '#/components/schemas/RolePermissions'

    UpdateRoleRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        color:
          type: string
        permissions:
          $ref: '#/components/schemas/RolePermissions'

    UserRole:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        assigned_at:
          type: string
          format: date-time

    AssignRoleRequest:
      type: object
      required: [role_id]
      properties:
        role_id:
          type: string
          format: uuid

    Emoji:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        format:
          type: string
          enum: [GIF, JPEG, JPG, PNG, WEBP]
        image_blob:
          type: string
          format: byte
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time

    CreateEmojiRequest:
      type: object
      required: [name, image_blob, format]
      description: Base64 de GIF, JPEG/JPG, PNG ou WEBP (máx. 2MB, 2048px); vazio remove o banner
      properties:
        name:
          type: string
        image_blob:
          type: string
          format: byte
        format:
          type: string
          enum: [GIF, JPEG, JPG, PNG, WEBP]

    SearchResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: '#/components/schemas/SearchResult'
        has_more:
          type: boolean
          description: Indica se existe próxima página (cursor since + last_id)

    SearchRequest:
      type: object
      description: Pelo menos 1 campo obrigatório; os filtros podem ser combinados
      properties:
        text:
          type: string
          description: Termo de busca
        author:
          type: string
          format: uuid
          description: Filtro por autor da mensagem
        order:
          type: string
          enum: [asc, desc]
          description: Ordem dos resultados por data
        date_start:
          type: string
          format: date
          description: Data inicial do filtro (inclusive)
        date_end:
          type: string
          format: date
          description: Data final do filtro (inclusive)
        contains_attachment:
          type: boolean
          description: Filtra mensagens com attachment

    SearchResult:
      type: object
      properties:
        type:
          type: string
          example: message
        id:
          type: string
          format: uuid
        content:
          type: string
        channel_id:
          type: string
          format: uuid
        channel_name:
          type: string
        author_id:
          type: string
          format: uuid
        author_username:
          type: string
        created_at:
          type: string
          format: date-time
        score:
          type: number
          format: double

    AuditLogEntry:
      type: object
      properties:
        id:
          type: string
          format: uuid
        actor_username:
          type: string
          description: Snapshot do username do ator no momento da operação
        action:
          type: string
          description: 'Ação auditável (ex.: user.ban, message.delete, role.create)'
        entity_type:
          type: string
          description: Tipo da entidade afetada (user, server, channel, message, attachment, role, user_role, emoji)
        target_user_id:
          type: string
          format: uuid
          nullable: true
          description: Usuário alvo da ação, quando difere do ator
        metadata:
          type: object
          additionalProperties: true
          description: Dados específicos da ação
        created_at:
          type: string
          format: date-time

    AuditLogList:
      type: object
      properties:
        logs:
          type: array
          items:
            $ref: '#/components/schemas/AuditLogEntry'
        has_more:
          type: boolean
          description: Indica se existe próxima página (cursor since + last_id)